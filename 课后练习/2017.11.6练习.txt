1、编写一个程序，求出整数数组中最小元素的下标。如果这样的元素个数大于1，则返回下标最小的数的下标。

2、有15个0~5之间的数字，分别统计0~5这之间的每个数字分别出现了多少次。

3、定义一个整数数组，求出其中的奇数个数和偶数个数。

4、定义一个整型数组，求出数组元素的和、数组元素的最大值和最小值，并输出所求结果。

5、需求：（二维数组实现）
	一、Hotel类
		// 5层楼，每层5个房间
		// 1-2层为标准间
		// 3-4层为双人间
		// 5层为豪华间
		方法:
		// 提供打印输出所有房间
		// 提供订房的方法
		// 提供退房的方法	
	二、HotelRoom类
		String no;// 房间号
		String type;// 标准间，双人间，豪华间
		boolean isUse;// true表示占用，false表示可以使用
	三、Test类
		main()
		Scanner类
